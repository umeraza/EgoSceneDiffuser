# Results

Evaluation commands write JSON metrics here or under the configured output directory. Keep generated
results separate from the manuscript's reported values in `paper/reported_*.csv`. A number belongs in
a reproduced-results table only when accompanied by the exact checkpoint, config, split file, commit,
hardware, and command.
