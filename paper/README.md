# Paper-derived records

These CSV files transcribe values stated in the supplied manuscript. They are **not outputs of this
repository** and are not independently verified. Contradictions are retained and flagged rather than
silently corrected. See `docs/REPRODUCIBILITY_AUDIT.md`.
